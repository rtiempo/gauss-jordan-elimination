import * as React from 'react';

const Specification = () => {
    return (
        <section id="specification">
            <h1>Specification</h1>
            <p>Insert Specification</p>
        </section> 
    );
};

export default Specification;